# 凡思課堂安裝

課堂側邊欄：檢查／安裝環境工具（uv／git／Node），並一鍵執行本課 `classroom-installs.yaml` 中的安裝動作。

**發佈方式**：VSIX 側載（MVP 不上架市集）。Cursor 相容需在目標版本實測，文件不保證與 VS Code 完全一致。

安裝後於活動列開啟「凡思課堂安裝」。使用步驟與打包指令見專案根目錄 README「課堂使用（VSIX 側載）」。
